extends layout

block content
  .w3-card-4
    header.w3-container.w3-red
      h2
        i.fas.fa-exclamation-triangle
        |  Ocorreu um erro
    .w3-container
      .w3-panel.w3-red.w3-padding
        h3= message
        if error
          pre= error.stack || error
      a.w3-button.w3-teal(href="/")
        i.fas.fa-home
        |  Voltar à página inicial